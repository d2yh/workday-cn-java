# Sample 02：放在 classpath 上的用户覆盖配置。
# 库加载顺序：内置 holiday-default.properties → 本文件（后者覆盖前者）。

# 无数据年份的回退策略改为 FESTIVAL（周末 + 春节 + 国庆）
data-calc.fallback.strategy=FESTIVAL

# 数据缓存目录（避免示例间互相污染）
data-store.dir=./sample-data
